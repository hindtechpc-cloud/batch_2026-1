export const formateFileContent = (user) =>
  `Hello ${user.name}, Your age is : ${user.age}, salary : ${user.salary} and you are a ${user.role} `;

// const formateFileContent = (user) =>
//   `Hello ${user.name}, Your age is : ${user.age}, salary : ${user.salary} and you are a ${user.role} `;
// export default formateFileContent;

// export default function formatFileContent(user) {
//   return `Hello ${user.name}, Your age is : ${user.age}, salary : ${user.salary} and you are a ${user.role} `;
// }

//  function formatFileContent(user) {
//   return `Hello ${user.name}, Your age is : ${user.age}, salary : ${user.salary} and you are a ${user.role} `;
// }
// export default formatFileContent;
